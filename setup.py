from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Trying to build an RNN from scratch to predict any sequence data',
    author='Espoir Murhabazi',
    license='',
)
